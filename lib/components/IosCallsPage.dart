import 'package:flutter/cupertino.dart';

class IosCallsPage extends StatelessWidget {
  const IosCallsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("CallPage"));
  }
}
